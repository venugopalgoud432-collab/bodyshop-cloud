services:
  - type: web
    name: bodyshop-cloud-v4
    env: node
    plan: starter
    buildCommand: "npm install"
    startCommand: "npm start"
    autoDeploy: true

databases:
  - name: bodyshop-cloud-v4-db
    plan: basic-256mb
